[run]
branch = True

[report]
exclude_lines =
  # Re-enable the standard pragma
  pragma: no cover

  # Don't complain if non-runnable code isn't run:
  if __name__ == .__main__.:

show_missing = True
fail_under = 100
